let COM = {
    Logger: {
        info: function (info,) {
            Logger.info(info, params);
        },
        debug: function (info,) {
            Logger.debug(info, params);
        },
        warn: function (info,) {
            Logger.warn(info, params);
        },
        error: function (info,) {
            Logger.error(info, params);
        }
    }
}